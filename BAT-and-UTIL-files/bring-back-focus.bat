@Echo OFF

 window restore
:window topmost
:window top
:activate restore
:activate top


rem 2024 due to plugins and help with the TCC form, window.bat is a BAT file which wraps the *window command
rem in functionality that uses a plugin to fix 'window restore' for windows terminal. Check it out.

rem May need to ask for the plugin dll as it may not be in my files due to lack of consent/permission 

